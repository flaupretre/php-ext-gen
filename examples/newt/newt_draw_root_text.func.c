# void newt_draw_root_text(int $left, int $top, string $text)

return_type: null

arguments:
  left:
    type: int
  top:
    type: int
  text:
    type: string

%%

newtDrawRootText ((long)left, (long)top, text_val);

